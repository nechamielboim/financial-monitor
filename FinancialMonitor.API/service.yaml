apiVersion: v1
kind: Service

metadata:
  name: financial-monitor-service

spec:
  type: LoadBalancer

  selector:
    app: financial-monitor-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080